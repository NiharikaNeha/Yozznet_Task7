import 'package:flutter/material.dart';

class P2P extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: (double.infinity)- 390.0,
      width: double.infinity,
      child: Center(
        child: Text('P2P'),
      )
    );
  }
}